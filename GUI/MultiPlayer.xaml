<Window x:Class="GUI.MultiPlayer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="MultiPlayer" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Label FontSize="18">List of games:</Label>
            <ComboBox x:Name="ListOfGames" Margin="5"></ComboBox>
        </DockPanel>
        <Button Content="Join Game" Click="JoinGame_Click" Width="100" FontSize="20" Grid.Row="1" Margin="5"/>
        <local:BasicUserControl x:Name="SUC" Grid.Row="2"/>
        <DockPanel Grid.Row="3">
            <Button Content="Start Game" FontSize="20" Click="StartGame_Click" Width="120" Margin="5"/>
        </DockPanel>
    </Grid>
</Window>
