<Window x:Class="GUI.SingleMaze"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:Controls="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="SingleMaze" Height="300" Width="300">
    <Viewbox>
        <Controls:MazeUC Rows="{Binding MazeVM.Rows}" Cols="{Binding MazeVM.Cols}" MazeName="{Binding MazeVM.Name}" MazeString="{Binding MazeVM.ToString()}"></Controls:MazeUC>
    </Viewbox>

</Window>
