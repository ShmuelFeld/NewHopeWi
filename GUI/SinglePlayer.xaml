<Window x:Class="GUI.SinglePlayer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:controls="clr-namespace:GUI"
        Title="SinglePlayer" Height="187.234" Width="300">
    <Viewbox>
        <Grid TextBlock.FontSize="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <controls:BasicUserControl x:Name="SUC" Grid.Row="0" Grid.Column="0">
            </controls:BasicUserControl>
            <Button Grid.Row="1" Height="30" Width="90" Margin="10,10,10,10" Click="btnStart_Click" Background="#FFD15353">
                Start Game</Button>

        </Grid>
    </Viewbox>
</Window>